node_modules/
dist/
*.zip
.DS_Store

# 本机的 Claude Code 配置，不进仓库
.claude/

# 根目录下不会有这些类型的主题文件，防止调试时抓下来的页面被误提交
/*.html
/*.xml

# 更新助手插件：编译依赖（从 Halo 的 jar 里抽出来的）和构建产物不进仓库
/plugins/*/lib/
/plugins/*/build/
