# 更新日志

后台装了「theme-x 助手」插件的话，点「更新」时弹出的确认框里会显示对应版本的这一节。
每一节的标题只写版本号（`## 1.2.3`），插件按这个找。

## 1.25.0

- **右栏文章大纲**：宽屏上文章页右栏最上面多一张「文章大纲」，列出正文里的标题，点一下跳过去，读到哪一段亮哪一条，往下翻时一直钉在搜索框下面
- 正文开头那个和文章标题一模一样的一级标题不会出现在大纲里；标题少于两个的文章不显示大纲
- 有大纲时宽屏上正文里的目录不再重复显示；手机没有右栏，照旧用正文里的目录
- 不想要的话在「主题设置 → 文章 → 右栏文章大纲」里关掉

## 1.24.0

- **theme-x 助手插件也能在线一键更新了**（插件 2.1.0）：有新版时，「插件」列表里它那一行会出现「更新到 x.y.z」按钮，仪表盘「theme-x 更新」部件里也有；插件设置保留
- 更新主题的确认框里多了「同时把 theme-x 助手插件更新到 x.y.z」的勾选框，点一次主题和插件一起更新
- 这一次还要手动升一次插件（2.0.0 还没有这个功能）：「插件」列表里 theme-x 助手那一行的「⋯ → 升级」，远程下载填 `https://你的域名/themes/theme-x/assets/plugins/theme-x-updater.jar`。以后就都能在线点了

## 1.23.1

- **两个配套插件并成一个**：「上传自动转 WebP」并进了「theme-x 助手」（插件 2.0.0），以后只装这一个。插件名称没变，直接升级，原来的设置都保留
- 升级插件：「插件」列表里 theme-x 助手那一行的「⋯ → 升级」，远程下载填 `https://你的域名/themes/theme-x/assets/plugins/theme-x-updater.jar`
- 升级后进一次后台会弹框提示卸载旧的「上传自动转 WebP」插件，点「卸载旧插件」会先把它的设置（质量、最长边、多小不转、提示）搬过来再卸载
- WebP 的设置挪到了「插件 → theme-x 助手 → 设置 → 上传转 WebP」

## 1.23.0

接入 api.miao.club 的几个小功能，全部默认关，在主题设置里一项项打开：

- **全网热榜**：「右栏 → 趋势卡片」选「全网热榜」，「有什么新鲜事」就显示微博、知乎、B 站、GitHub、少数派、Hacker News 等的实时热榜，勾几个来源就有几个按钮可以切换
- **历史上的今天**：「右栏 → 『历史上的今天』卡片」，右栏多一张卡片，点「显示更多」展开全部
- **发帖框里的一句话**：「首页 → 发帖框里换成一句话」，把「有什么新鲜事？」换成随机一言或古诗词
- **正文链接卡片**：「文章 → 正文链接变成卡片」，单独占一段的链接变成带标题、摘要、配图的卡片
- **友链申请自动填写**：「友链页 → 申请表单自动填写」，访客填好网址就按对方网站的信息填好名称、简介、头像
- **友链体检**（「theme-x 助手」插件 1.2.0）：后台「内容 → 友链体检」，定时检查每个友链还能不能打开，连续几次打不开的标成失联。插件要单独升级，见下

升级插件：「插件」列表里 theme-x 助手那一行的「⋯ → 升级」，远程下载填 `https://你的域名/themes/theme-x/assets/plugins/theme-x-updater.jar`（主题先升到 1.23.0）。

## 1.22.0

- **Epic 限免 → 游戏限免**：「主题设置 → 游戏限免 → 显示哪些平台」可以勾 Epic / Steam / GOG，多选就合在一张卡片里，每行标出是哪家
- 卡片标题没填时按勾选自动起名：只勾 Epic 还叫「Epic 限免」，只勾一家别的叫「Steam 限免」「GOG 限免」，多选叫「游戏限免」
- Steam / GOG 的数据来自 api.miao.club；GOG 和大部分 Steam 限免不给截止时间，就只写「限免中」；某一家接口挂了，其它几家照常显示
- 「游戏限免」页面模板同步：顶部写勾了哪几家，拿不到数据时每家商店给一个按钮
- 不改设置的话一切照旧（默认只勾 Epic）

## 1.21.1

- **Epic 限免接入 api.miao.club**：内置数据源的第一个换成 `https://api.miao.club/api/epic/free`，「数据接口」留空就用它，原来的几个公共接口排在后面兜底
- 认得这个接口的新字段（`originalPrice` / `startDate` / `endDate` / `image.wide`），旧版聚合 API 的写法照样认
- 「数据接口」里填的地址要是和内置的重复，不会再被请求两遍

## 1.21.0

- **名字和头像只需要设一次**：文章和瞬间卡片默认也用「主题设置 → 个人资料」里的头像、显示名、用户名，和左栏保持一致；Halo 账号里的显示名称和头像不用再改
- 多人博客可以在「个人资料 → 文章卡片上的署名」里改成「用各文章作者的 Halo 账号」，恢复以前的样子
- 注意：升级后你的文章卡片会从「Miao @miaoclub」变成和左栏一样的「Miao's Blog @Miao」——想换名字就改主题设置里的那几项

## 1.20.0

- 导航去重：个人主页那排标签去掉「列表」「话题」（它们点开就是分类页和标签页，和左栏重复），现在只剩「帖子 / 媒体 / 书签」；分类页、标签页上不再显示这排标签
- 只剩「帖子」一格时整排不画
- 左栏收起 / 展开变成平滑过渡（约 260ms），文字淡出，中栏宽度同步跟着滑；系统开了「减少动态效果」则瞬间切换
- 随主题带的「上传自动转 WebP」升到 1.1.0：不再只认写死的那两个上传地址，改成「同源 + 路径里带 attachment 的 POST」都拦，并且 fetch 上传也接住了（Halo Pro 后台走的可能是这条）；控制台会打一行就绪日志，`window.__webpUpload.seen` 能看到每次上传的判定结果

## 1.19.0

- 「申请友链」搬到右栏（只在友链页出现）：友链再多也不会把申请入口挤下去；窄屏右栏是隐藏的，这块还是落在页面底部
- 修掉一个隐蔽问题：`th:replace` 的优先级高于 `th:if`，写在同一个标签上条件不生效——右栏那份差点在每个页面都出现

## 1.18.1

- 「收起侧边栏」现在是一键切成博客的样子：左栏收成图标条、**中栏把省下来的宽度全吃掉**（600 → 787px，三栏位置和两边留白都不动，不再右边空出一条）、文章列表切成博客样式、顶上的发帖框收起
- 宽栏下缩略图放大到 168×112，标题也大一档
- 再点一次「展开侧边栏」就回到 X 的样子；想单独调列表样式，标签栏右边的按钮和「显示」面板照旧管用

## 1.17.2

- 左栏加了「收起侧边栏」：宽屏下把左栏收成纯图标条，再点一次展开，选择记在访客浏览器里（窄屏本来就是图标条，按钮不显示）
- 文章没设封面时，列表里的封面自动用正文里的第一张图；可在「文章 → 没设封面时用正文首图」里关掉。实测首页渲染只多约 6ms

## 1.16.1

- 文章列表可以切两种样式了：**X 时间线**（现在这样）和**博客列表**（大标题、两行摘要、右侧缩略图、没有头像和互动栏）。访客自己选，存在他自己浏览器里
- 入口两处：首页标签栏右边的小按钮，「显示」面板里的「列表样式」
- 站长可以在「外观 → 文章列表样式」里设默认值（默认仍是 X 时间线，老访客看到的东西不变）
- 作用范围：首页、探索、分类、标签、搜索结果；友链动态、瞬间、媒体页保持原样
- 博客列表里日期显示成「9月19日」这种绝对日期，X 时间线仍是「4天」

## 1.15.0

- 友链页底部加了「申请友链」：上半部分是你自己的友链信息（名称、地址、头像、描述，一键复制给对方），下半部分是申请表单（名称、地址、头像、描述、RSS、邮箱 + 图形验证码），提交直接进后台「链接 → 友链申请」等审核
- 表单走「链接」插件的公开接口，需要在插件设置「友链申请」里打开「启用友链申请」和「允许访客提交」；没开的话访客点开只看到一句说明，你的信息照常显示
- 新增设置组「友链页」：入口开关、我的站点名称/地址/头像/描述（留空自动用站点信息）、申请须知

## 1.14.1

- 「正在关注」的友链动态排版收紧：标题最多两行、摘要最多两行且压成次要文字，卡片留白调匀，不再一屏糊成一片
- 摘要里别人 RSS 带的 Markdown 符号（`**加粗**`、`[标题](链接)`、`#`、反引号）清掉，只留文字

## 1.14.0

- **置顶改用 Halo 自己的**：在「文章」列表里把文章设为置顶就行，首页会把它排在最前并带上「已置顶」标记。主题设置里的「置顶文章」已经删掉——原来在那儿选过的，改到文章管理里置顶一次即可
- 可以同时置顶多篇，都会排在前面

## 1.13.12

- 「正在关注」拿不到友链动态时，在浏览器控制台写一句原因（多半是链接插件里的「公开 RSS 订阅动态」没开），页面上照旧安静地退回友链列表

## 1.13.11

- 时间线摘要里的网址现在是蓝色可点的链接（和 X 一样），显示时去掉 http:// 前缀、太长的截断；站外链接新标签页打开
- 点链接不会再顺带打开这篇文章

## 1.13.10

- 首页标签页不再被永久记住：重新打开博客一定从第一个标签页（自己的文章）开始，站内点来点去、刷新仍然保持你刚选的那个。之前点过一次「正在关注」就会一直停在那里
- 升级后会自动清掉旧的记录

## 1.13.9

- 随主题带的插件升到 1.1.0，改名「theme-x 助手」：多了一页「内容 → 友链 RSS」，点「一键发现并开启」就会挨个访问友链的网站、自动找出各自的 RSS 地址、填好开启并立刻抓一次，「公开 RSS 订阅动态」那个开关也能在同一页点开。RSS 地址不用自己填了
- 装过旧版（1.0.0）的话，在「插件 → 安装 → 远程下载」里用同一个地址重装一次即可

## 1.13.8

- 首页第二个标签页（默认叫「正在关注」）可以选「友链」了：显示朋友们的最新文章（需要给友链填 RSS 并在链接插件里打开「公开 RSS 订阅动态」），取不到就列友链本身。在「首页 → 第二个标签页取什么内容」里选
- 还是点开才加载，不影响首屏

## 1.13.7

- 多带一个插件「上传自动转 WebP」：后台传图片时先在浏览器里转成 WebP 再传，体积通常只剩三分之一到十分之一，服务器什么都不用装（装法见 README）
- 两个插件的 jar 统一挪到 `assets/plugins/` 下：更新助手的地址改成 `/themes/theme-x/assets/plugins/theme-x-updater.jar`

## 1.13.6

- 手机上手指往下一拖，收起的顶栏和底栏马上回来，不用等页面真的滚动（以前在 Chrome 里要先把浏览器地址栏拉出来，栏才出来，容易一路拽到下拉刷新）
- 默认拦掉手机浏览器的下拉刷新，免得误触整页刷新；想要可以在「功能 → 手机浏览器下拉刷新」里改回「保留」
- 新增「功能 → 手机上滚动时收起顶栏和底栏」开关，关掉则两条栏一直显示

## 1.13.5

- 书签、分享两个按钮不再挤在最右边，和评论、转发、喜欢、浏览一样等距排开
- 主题里附带「theme-x 更新助手」插件：装一次以后，后台有新版本会提示，点「更新到 x.y.z」就能升级
- 主题详情里补上源码仓库、主页和问题反馈的地址

## 1.13.4

- 点「书签」切页不再闪一下：没建书签页的站点也能无刷新切过去
- 书签页（包括没建书签页时的兜底页）也带上个人主页那排标签
- 书签和分享之间留出间距
