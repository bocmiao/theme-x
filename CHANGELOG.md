# 更新日志

后台装了「theme-x 助手」插件的话，点「更新」时弹出的确认框里会显示对应版本的这一节。
每一节的标题只写版本号（`## 1.2.3`），插件按这个找。

## 1.13.12

- 「正在关注」拿不到友链动态时，在浏览器控制台写一句原因（多半是链接插件里的「公开 RSS 订阅动态」没开），页面上照旧安静地退回友链列表

## 1.13.11

- 时间线摘要里的网址现在是蓝色可点的链接（和 X 一样），显示时去掉 http:// 前缀、太长的截断；站外链接新标签页打开
- 点链接不会再顺带打开这篇文章

## 1.13.10

- 首页标签页不再被永久记住：重新打开博客一定从第一个标签页（自己的文章）开始，站内点来点去、刷新仍然保持你刚选的那个。之前点过一次「正在关注」就会一直停在那里
- 升级后会自动清掉旧的记录

## 1.13.9

- 随主题带的插件升到 1.1.0，改名「theme-x 助手」：多了一页「内容 → 友链 RSS」，点「一键发现并开启」就会挨个访问友链的网站、自动找出各自的 RSS 地址、填好开启并立刻抓一次，「公开 RSS 订阅动态」那个开关也能在同一页点开。RSS 地址不用自己填了
- 装过旧版（1.0.0）的话，在「插件 → 安装 → 远程下载」里用同一个地址重装一次即可

## 1.13.8

- 首页第二个标签页（默认叫「正在关注」）可以选「友链」了：显示朋友们的最新文章（需要给友链填 RSS 并在链接插件里打开「公开 RSS 订阅动态」），取不到就列友链本身。在「首页 → 第二个标签页取什么内容」里选
- 还是点开才加载，不影响首屏

## 1.13.7

- 多带一个插件「上传自动转 WebP」：后台传图片时先在浏览器里转成 WebP 再传，体积通常只剩三分之一到十分之一，服务器什么都不用装（装法见 README）
- 两个插件的 jar 统一挪到 `assets/plugins/` 下：更新助手的地址改成 `/themes/theme-x/assets/plugins/theme-x-updater.jar`

## 1.13.6

- 手机上手指往下一拖，收起的顶栏和底栏马上回来，不用等页面真的滚动（以前在 Chrome 里要先把浏览器地址栏拉出来，栏才出来，容易一路拽到下拉刷新）
- 默认拦掉手机浏览器的下拉刷新，免得误触整页刷新；想要可以在「功能 → 手机浏览器下拉刷新」里改回「保留」
- 新增「功能 → 手机上滚动时收起顶栏和底栏」开关，关掉则两条栏一直显示

## 1.13.5

- 书签、分享两个按钮不再挤在最右边，和评论、转发、喜欢、浏览一样等距排开
- 主题里附带「theme-x 更新助手」插件：装一次以后，后台有新版本会提示，点「更新到 x.y.z」就能升级
- 主题详情里补上源码仓库、主页和问题反馈的地址

## 1.13.4

- 点「书签」切页不再闪一下：没建书签页的站点也能无刷新切过去
- 书签页（包括没建书签页时的兜底页）也带上个人主页那排标签
- 书签和分享之间留出间距
